import UnderConstruction from "../components/reusables/UnderConstruction"

type Props = {}

export const PageSup = (props: Props) => {
  return (
    <section>
        <h2>PageSup</h2>
        <UnderConstruction />
    </section>
  )
}